/** Aggregates belong to the processor read model, not the EE map snapshot. */
export default function LiveLine() {
  return <p className="text-xs text-slate-600 dark:text-slate-300">Статистика оброблених повідомлень за обраний період. Карта й каталог показують окремі результати витягання сутностей; їхні лічильники можуть відрізнятися.</p>
}
