{
  "$comment": "Правило: однаковий MAJOR → compatible (невідомі поля ігноруються, нові optional поля дозволені); інший MAJOR або невалідна версія → quarantine, не exception (§5.1).",
  "rule": "major_equal",
  "consumer_supported": {
    "raw.stored": "1.0",
    "message.analysis.completed": "1.0"
  },
  "cases": [
    {
      "event_type": "raw.stored",
      "event_schema_version": "1.0",
      "decision": "compatible"
    },
    {
      "event_type": "raw.stored",
      "event_schema_version": "1.1",
      "decision": "compatible",
      "fixture": "valid/raw.stored.additive-1.1.json"
    },
    {
      "event_type": "raw.stored",
      "event_schema_version": "1.3",
      "decision": "compatible"
    },
    {
      "event_type": "raw.stored",
      "event_schema_version": "2.0",
      "decision": "quarantine"
    },
    {
      "event_type": "raw.stored",
      "event_schema_version": "0.9",
      "decision": "quarantine"
    },
    {
      "event_type": "raw.stored",
      "event_schema_version": "v1",
      "decision": "quarantine"
    },
    {
      "event_type": "message.analysis.completed",
      "event_schema_version": "1.0",
      "decision": "compatible"
    },
    {
      "event_type": "unknown.event",
      "event_schema_version": "1.0",
      "decision": "quarantine"
    }
  ],
  "breaking_examples": [
    {
      "change": "видалити required поле raw.stored.payload.content_hash",
      "expected": "invalid проти 1.x схеми; потребує MAJOR 2"
    },
    {
      "change": "змінити тип raw.stored.payload.raw_message_id на string",
      "expected": "invalid проти 1.x схеми; потребує MAJOR 2"
    }
  ]
}
